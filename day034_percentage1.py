# write a program that reads the percentage p and prints the percentage p of number 200.
percentage = float(input("Enter the percentage: "))
result = (percentage / 100)* 200
print("Percentage", percentage, "of 200 is:", result)
